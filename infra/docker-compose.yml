services:
  app1: &app-config
    build: ..
    ports:
      - "8081:8080"
    environment:
      - PAYMENT_GATEWAY_DEFAULT=http://payment-processor-default:8080
      - PAYMENT_GATEWAY_FALLBACK=http://payment-processor-fallback:8080
      - SUMMARY_SERVICE_URL=http://app2:8080
    networks:
      - backend
      - payment-processor
#    deploy:
#      resources:
#        limits:
#          cpus: "4.0"
#          memory: "4000MB"
    restart: unless-stopped

  app2:
    <<: *app-config
    environment:
      - PAYMENT_GATEWAY_DEFAULT=http://payment-processor-default:8080
      - PAYMENT_GATEWAY_FALLBACK=http://payment-processor-fallback:8080
      - SUMMARY_SERVICE_URL=http://app1:8080
    ports:
      - "8082:8080"

  load-balancer:
    image: nginx:alpine
    ports:
      - "9999:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    networks:
      - backend
    deploy:
      resources:
        limits:
#          cpus: "2"
          memory: "10MB"
    depends_on:
      - app1
      - app2

networks:
  backend:
    driver: bridge
  payment-processor:
    external: true